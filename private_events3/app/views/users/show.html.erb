<h3>Profile: <%= current_user.email %></h3>
<%= button_to "Log Out", destroy_user_session_path, method: :delete %>

<h1>My events: </h1>

<%= link_to "[New Event]", new_party_path %>


<div>
    <h4>This are the events you have created: </h4>
    <ul>
        <% @parties.each do |party| %>
            <li>
                <div>
                    <p>
                        <strong>Where?: </strong> <%= party.place %>
                    </p>
                    <p>
                        <strong>When?: </strong> <%= party.date %>
                    </p>
                </div>
                <%= link_to "[Show Event]",  party_path(party)%>
                <%= link_to "[Delete]",  party_path(party)%>
            </li>
        <% end %>
    </ul>
</div>

<%= link_to "[Go Back]", root_path %>
